// Code generated by Polytomic. DO NOT EDIT.
// edit connections.yaml and re-run go generate

package provider

import (
	"context"
	"fmt"
	"regexp"
	"testing"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/resource"
	"github.com/hashicorp/terraform-plugin-sdk/v2/terraform"
)

func TestAccMysqlConnection(t *testing.T) {
	name := "TestAcc"
	resource.Test(t, resource.TestCase{
		PreCheck:                 func() { testAccPreCheck(t) },
		ProtoV6ProviderFactories: testAccProtoV6ProviderFactories,
		Steps: []resource.TestStep{
			// Missing configuration
			{
				Config:      testAccMysqlConnectionResourceNoConfig(name),
				ExpectError: regexp.MustCompile("The argument \"configuration\" is required, but no definition was found."),
			},
			// Empty configuration
			{
				Config:      testAccMysqlConnectionResourceMissingRequired(name),
				ExpectError: regexp.MustCompile("Inappropriate value for attribute \"configuration\":"),
			},
			{
				Config: testAccMysqlConnectionResource(name),
				Check: resource.ComposeTestCheckFunc(
					// Check if the resource exists
					testAccMysqlConnectionExists(name),
					// Check the name
					resource.TestCheckResourceAttr("polytomic_mysql_connection.test", "name", name),
				),
			},
		},
	})
}

func testAccMysqlConnectionExists(name string) resource.TestCheckFunc {
	return func(s *terraform.State) error {
		_, ok := s.RootModule().Resources["polytomic_mysql_connection.test"]
		if !ok {
			return fmt.Errorf("not found: %s", "polytomic_mysql_connection.test")
		}

		client := testClient()
		connections, err := client.Connections().List(context.TODO())
		if err != nil {
			return err
		}
		var found bool
		for _, connection := range connections {
			if connection.Name == name {
				found = true
				break
			}
		}

		if !found {
			return fmt.Errorf("connection %s not found", name)
		}

		return nil

	}
}

func testAccMysqlConnectionResource(name string) string {
	return fmt.Sprintf(`
resource "polytomic_mysql_connection" "test" {
	name          = "%s"
	configuration = {
   hostname = "mysql.example.com"
   account = "acme"
   passwd = "super-secret-password"
   dbname = "db"
   port = 3306
   }
}
`, name)
}

func testAccMysqlConnectionResourceNoConfig(name string) string {
	return fmt.Sprintf(`
resource "polytomic_mysql_connection" "test" {
	name          = "%s"
}
`, name)
}

func testAccMysqlConnectionResourceMissingRequired(name string) string {
	return fmt.Sprintf(`
resource "polytomic_mysql_connection" "test" {
	name          = "%s"
	 configuration = {}
}`, name)
}
