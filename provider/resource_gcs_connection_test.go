// Code generated by Polytomic. DO NOT EDIT.
// edit connections.yaml and re-run go generate

package provider

import (
	"context"
	"fmt"
	"regexp"
	"testing"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/resource"
	"github.com/hashicorp/terraform-plugin-sdk/v2/terraform"
)

func TestAccGcsConnection(t *testing.T) {
	name := "TestAcc"
	resource.Test(t, resource.TestCase{
		PreCheck:                 func() { testAccPreCheck(t) },
		ProtoV6ProviderFactories: testAccProtoV6ProviderFactories,
		Steps: []resource.TestStep{
			// Missing configuration
			{
				Config:      testAccGcsConnectionResourceNoConfig(name),
				ExpectError: regexp.MustCompile("The argument \"configuration\" is required, but no definition was found."),
			},
			// Empty configuration
			{
				Config:      testAccGcsConnectionResourceMissingRequired(name),
				ExpectError: regexp.MustCompile("Inappropriate value for attribute \"configuration\":"),
			},
			{
				Config: testAccGcsConnectionResource(name),
				Check: resource.ComposeTestCheckFunc(
					// Check if the resource exists
					testAccGcsConnectionExists(name),
					// Check the name
					resource.TestCheckResourceAttr("polytomic_gcs_connection.test", "name", name),
				),
			},
		},
	})
}

func testAccGcsConnectionExists(name string) resource.TestCheckFunc {
	return func(s *terraform.State) error {
		_, ok := s.RootModule().Resources["polytomic_gcs_connection.test"]
		if !ok {
			return fmt.Errorf("not found: %s", "polytomic_gcs_connection.test")
		}

		client := testClient()
		connections, err := client.Connections().List(context.TODO())
		if err != nil {
			return err
		}
		var found bool
		for _, connection := range connections {
			if connection.Name == name {
				found = true
				break
			}
		}

		if !found {
			return fmt.Errorf("connection %s not found", name)
		}

		return nil

	}
}

func testAccGcsConnectionResource(name string) string {
	return fmt.Sprintf(`
resource "polytomic_gcs_connection" "test" {
	name          = "%s"
	configuration = {
   project_id = "my-project"
   service_account = "data.account_credentials.json"
   bucket = "my-bucket"
   }
}
`, name)
}

func testAccGcsConnectionResourceNoConfig(name string) string {
	return fmt.Sprintf(`
resource "polytomic_gcs_connection" "test" {
	name          = "%s"
}
`, name)
}

func testAccGcsConnectionResourceMissingRequired(name string) string {
	return fmt.Sprintf(`
resource "polytomic_gcs_connection" "test" {
	name          = "%s"
	 configuration = {}
}`, name)
}
