// Code generated by Polytomic. DO NOT EDIT.
// edit connections.yaml and re-run go generate

package provider

import (
	"context"
	"fmt"
	"regexp"
	"testing"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/resource"
	"github.com/hashicorp/terraform-plugin-sdk/v2/terraform"
)

func TestAccCustomerioConnection(t *testing.T) {
	name := "TestAcc"
	resource.Test(t, resource.TestCase{
		PreCheck:                 func() { testAccPreCheck(t) },
		ProtoV6ProviderFactories: testAccProtoV6ProviderFactories,
		Steps: []resource.TestStep{
			// Missing configuration
			{
				Config:      testAccCustomerioConnectionResourceNoConfig(name),
				ExpectError: regexp.MustCompile("The argument \"configuration\" is required, but no definition was found."),
			},
			// Empty configuration
			{
				Config:      testAccCustomerioConnectionResourceMissingRequired(name),
				ExpectError: regexp.MustCompile("Inappropriate value for attribute \"configuration\":"),
			},
			{
				Config: testAccCustomerioConnectionResource(name),
				Check: resource.ComposeTestCheckFunc(
					// Check if the resource exists
					testAccCustomerioConnectionExists(name),
					// Check the name
					resource.TestCheckResourceAttr("polytomic_customerio_connection.test", "name", name),
				),
			},
		},
	})
}

func testAccCustomerioConnectionExists(name string) resource.TestCheckFunc {
	return func(s *terraform.State) error {
		_, ok := s.RootModule().Resources["polytomic_customerio_connection.test"]
		if !ok {
			return fmt.Errorf("not found: %s", "polytomic_customerio_connection.test")
		}

		client := testClient()
		connections, err := client.Connections().List(context.TODO())
		if err != nil {
			return err
		}
		var found bool
		for _, connection := range connections {
			if connection.Name == name {
				found = true
				break
			}
		}

		if !found {
			return fmt.Errorf("connection %s not found", name)
		}

		return nil

	}
}

func testAccCustomerioConnectionResource(name string) string {
	return fmt.Sprintf(`
resource "polytomic_customerio_connection" "test" {
	name          = "%s"
	configuration = {
   site_id = "my-site-id"
   tracking_api_key = "my-tracking-api-key"
   app_api_key = "my-app-api-key"
   }
}
`, name)
}

func testAccCustomerioConnectionResourceNoConfig(name string) string {
	return fmt.Sprintf(`
resource "polytomic_customerio_connection" "test" {
	name          = "%s"
}
`, name)
}

func testAccCustomerioConnectionResourceMissingRequired(name string) string {
	return fmt.Sprintf(`
resource "polytomic_customerio_connection" "test" {
	name          = "%s"
	 configuration = {}
}`, name)
}
