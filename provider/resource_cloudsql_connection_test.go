// Code generated by Polytomic. DO NOT EDIT.
// edit connections.yaml and re-run go generate

package provider

import (
	"context"
	"fmt"
	"regexp"
	"testing"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/resource"
	"github.com/hashicorp/terraform-plugin-sdk/v2/terraform"
)

func TestAccCloudsqlConnection(t *testing.T) {
	name := "TestAcc"
	resource.Test(t, resource.TestCase{
		PreCheck:                 func() { testAccPreCheck(t) },
		ProtoV6ProviderFactories: testAccProtoV6ProviderFactories,
		Steps: []resource.TestStep{
			// Missing configuration
			{
				Config:      testAccCloudsqlConnectionResourceNoConfig(name),
				ExpectError: regexp.MustCompile("The argument \"configuration\" is required, but no definition was found."),
			},
			// Empty configuration
			{
				Config:      testAccCloudsqlConnectionResourceMissingRequired(name),
				ExpectError: regexp.MustCompile("Inappropriate value for attribute \"configuration\":"),
			},
			{
				Config: testAccCloudsqlConnectionResource(name),
				Check: resource.ComposeTestCheckFunc(
					// Check if the resource exists
					testAccCloudsqlConnectionExists(name),
					// Check the name
					resource.TestCheckResourceAttr("polytomic_cloudsql_connection.test", "name", name),
				),
			},
		},
	})
}

func testAccCloudsqlConnectionExists(name string) resource.TestCheckFunc {
	return func(s *terraform.State) error {
		_, ok := s.RootModule().Resources["polytomic_cloudsql_connection.test"]
		if !ok {
			return fmt.Errorf("not found: %s", "polytomic_cloudsql_connection.test")
		}

		client := testClient()
		connections, err := client.Connections().List(context.TODO())
		if err != nil {
			return err
		}
		var found bool
		for _, connection := range connections {
			if connection.Name == name {
				found = true
				break
			}
		}

		if !found {
			return fmt.Errorf("connection %s not found", name)
		}

		return nil

	}
}

func testAccCloudsqlConnectionResource(name string) string {
	return fmt.Sprintf(`
resource "polytomic_cloudsql_connection" "test" {
	name          = "%s"
	configuration = {
   connection_name = "my-project:us-central1:my-instance"
   database = "my-db"
   username = "cloudsql"
   credentials = "data.account_credentials.json"
   }
}
`, name)
}

func testAccCloudsqlConnectionResourceNoConfig(name string) string {
	return fmt.Sprintf(`
resource "polytomic_cloudsql_connection" "test" {
	name          = "%s"
}
`, name)
}

func testAccCloudsqlConnectionResourceMissingRequired(name string) string {
	return fmt.Sprintf(`
resource "polytomic_cloudsql_connection" "test" {
	name          = "%s"
	 configuration = {}
}`, name)
}
